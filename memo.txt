生成AIがマークダウンの中にさらにコードブロック（```）を含むテキストを出力しようとすると、閉じタグを誤認識して表示が崩れることがよくあります。

これを回避するために、外側の枠を「バッククォート4つ（````）」で囲んで出力します。これなら中のコードブロック（バッククォート3つ）も文字として正しく表示されるはずです。

以下のボックスの右上の「Copy」ボタンを押して、README.md として保存してください。